# Production PostgreSQL
DATABASE_URL=

# Recommended
NODE_ENV=production

# Optional: only needed if the frontend is hosted separately
# CORS_ORIGIN=https://your-frontend.example.com

# Optional backup output directory
# BACKUP_DIR=/tmp/bluefarm-backups
